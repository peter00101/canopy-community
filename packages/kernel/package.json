{
  "name": "@canopy/kernel",
  "version": "0.1.13",
  "license": "AGPL-3.0-only",
  "description": "Canopy Agent 内核：Runtime 无关的通用工具（权限决策、错误归一化、项目指令解析、运行环境构建）与 Runtime 插件契约",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@canopy/brand": "workspace:*",
    "@canopy/session-core": "workspace:*",
    "@canopy/shared": "workspace:*",
    "typebox": "1.3.27"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
